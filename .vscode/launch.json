{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "attach",
            "name": "Attach",
            "waitFor": true,
            "linux": {
                "program": "hl2_linux"
            },
            "windows": {
                "program": "hl2.exe",
            },
            // "pid": "${command:pickMyProcess}" // use ${command:pickProcess} to pick other users' processes
        }
    ]
}